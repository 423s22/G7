import { TransportDispatch } from '..';
interface Payload {
    id: string;
    origin: string;
    data: TransportDispatch;
}
/**
 * Adds context to the MessageTransport payload to distinguish whether an action
 * is dispatched from Main or Modal.
 */
export declare function parsePayloadWithContext(payload: Payload): string;
export {};
